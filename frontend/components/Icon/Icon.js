// import PropTypes from 'prop-types';
import s from './Icon.module.css';

const Icon = () => {
    return <div className={s.Root}>Icon</div>;
};

Icon.propTypes = {};

Icon.defaultProps = {};

export default Icon;
